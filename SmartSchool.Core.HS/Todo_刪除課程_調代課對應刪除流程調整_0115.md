# Todo — 刪除課程：調代課對應判斷與「先刪對應再刪課」流程（0115）

## 目標
調整 `Manage.cs` 的 `btnDeleteCourse_Click`：
1. **若課程存在調代課對應（dc_bind_key）且使用者確認要刪除**，則 **不再顯示**「您確定將刪除此課程？」第二次確認，**直接進入刪除課程流程**。
2. 在確定要刪除課程且需要處理調代課對應時，必須先呼叫 `QueryData.DeleteCourseDCBindByCourseID(courseId)` 刪除 `dc_bind_key` 對應資料，刪除成功後再刪除課程。
3. 修改完成後，更新紀錄文件：`刪除課程功能調整0115.md`。

---

## 影響範圍
- `Manage.cs`
  - `btnDeleteCourse_Click(object sender, EventArgs e)`：刪除流程重整（新增「先刪對應再刪課」與取消第二次確認）
- `QueryData.cs`
  - 確認已存在/新增：`DeleteCourseDCBindByCourseID(string CousreID)`（刪除調代課對應並回傳刪除筆數）
  - 既有：`GetCourseDCBindCountByID(string CousreID)`（取得對應筆數）
- 文件
  - `刪除課程功能調整0115.md`

---

## Todo 1 — 確認 QueryData.cs 已具備刪除調代課對應 Method
### 檔案
- `QueryData.cs`

### 需確認內容
- [ ] 存在 `public static int DeleteCourseDCBindByCourseID(string CousreID)`
- [ ] 行為：
  - CousreID 非整數 → 回傳 `-1`
  - DB 例外 → 回傳 `-1`
  - 成功 → 回傳實際刪除筆數（0 代表沒有對應資料）
- [ ] 使用 `DELETE FROM dc_bind_key WHERE ref_course_id = {0} RETURNING id;` 並以 `dt.Rows.Count` 回傳刪除筆數（PostgreSQL OK）

---

## Todo 2 — 修改 Manage.cs：重整 btnDeleteCourse_Click 刪除流程
### 檔案
- `Manage.cs`

### 修改點摘要
保留並優先處理：
1) `attendCount > 0` → 顯示提示並 return（不進行 dcBindCount/刪除）

新增/調整：
2) 取得 `dcBindCount` 後：
- `dcBindCount < 0`：顯示「無法確認調代課對應」並 return（保守策略）
- `dcBindCount == 0`：**保留原本**「您確定將刪除此課程？」確認
- `dcBindCount > 0`：先顯示一次提示詢問是否仍要刪除；若使用者選 Yes：
   - 先呼叫 `QueryData.DeleteCourseDCBindByCourseID(...)`
   - 刪除失敗（<0）→ 顯示錯誤並 return（中止刪課）
   - 刪除成功（>=0）→ **不再顯示**「您確定將刪除此課程？」→ 直接刪除課程

### 建議替換程式碼（整段替換 btnDeleteCourse_Click）
```csharp
private void btnDeleteCourse_Click(object sender, EventArgs e)
{
    if (Course.Instance.SelectionCourse.Count <= 0) return;

    int deleteCourseID = Course.Instance.SelectionCourse[0].Identity;
    int attendCount = Course.Instance.FindSCAttendByCourseID(deleteCourseID);

    if (attendCount > 0)
    {
        MsgBox.Show("該課程目前修課學生人數為 " + attendCount + " 人，若要刪除課程請先移除修課學生。");
        return;
    }

    // 取得調代課對應筆數
    int dcBindCount = QueryData.GetCourseDCBindCountByID(deleteCourseID.ToString());

    // 若無法取得調代課對應筆數（例如查詢失敗/權限不足/連線問題），採保守策略：先提示並中止
    if (dcBindCount < 0)
    {
        MsgBox.Show("目前無法確認此課程是否存在調代課對應資料（查詢失敗）。為避免造成資料不一致，請稍後再試或請系統管理員確認資料庫狀態。");
        return;
    }

    // 有調代課對應：只做一次確認；同意後先刪對應再刪課（不再顯示第二次「確定刪課」）
    if (dcBindCount > 0)
    {
        var dr = MsgBox.Show(
            string.Format("此課程尚有 {0} 筆調代課對應資料。\n若要刪除課程，系統將先刪除調代課對應資料，再刪除課程。\n是否仍要繼續？", dcBindCount),
            "",
            MessageBoxButtons.YesNo
        );

        if (dr == DialogResult.No)
            return;

        try
        {
            // 先刪除調代課對應資料
            int deletedDCBind = QueryData.DeleteCourseDCBindByCourseID(deleteCourseID.ToString());
            if (deletedDCBind < 0)
            {
                MsgBox.Show("刪除調代課對應資料失敗，為避免造成資料不一致，已中止刪除課程。");
                return;
            }

            // 再刪除課程（不再顯示「您確定將刪除此課程？」）
            DeleteCourseHideAttend(deleteCourseID);

            string course_name = Course.Instance.SelectionCourse[0].CourseName;
            RemoveCourse.DeleteCourse(deleteCourseID.ToString());
            Course.Instance.InvokeAfterCourseDelete(deleteCourseID);

            // Log
            CurrentUser.Instance.AppLog.Write(
                SmartSchool.ApplicationLog.EntityType.Course,
                "刪除課程",
                deleteCourseID.ToString(),
                string.Format("課程「{0}」已刪除（已先刪除調代課對應 {1} 筆）", course_name, deletedDCBind),
                "課程",
                deleteCourseID.ToString()
            );
        }
        catch (Exception ex)
        {
            MsgBox.Show(ex.Message);
        }

        return; // 避免往下再走到原本刪除確認流程
    }

    // dcBindCount == 0：維持原本刪除確認
    if (MsgBox.Show("您確定將刪除此課程？", "", MessageBoxButtons.YesNo) == DialogResult.Yes)
    {
        try
        {
            DeleteCourseHideAttend(deleteCourseID);

            string course_name = Course.Instance.SelectionCourse[0].CourseName;
            RemoveCourse.DeleteCourse(deleteCourseID.ToString());
            Course.Instance.InvokeAfterCourseDelete(deleteCourseID);

            //Log
            CurrentUser.Instance.AppLog.Write(
                SmartSchool.ApplicationLog.EntityType.Course,
                "刪除課程",
                deleteCourseID.ToString(),
                string.Format("課程「{0}」已刪除", course_name),
                "課程",
                deleteCourseID.ToString()
            );
        }
        catch (Exception ex)
        {
            MsgBox.Show(ex.Message);
        }
    }
}
```

---

## Todo 3 — 測試案例（手動）
- [ ] A. attendCount > 0：擋刪，提示後中止
- [ ] B. attendCount == 0、dcBindCount == 0：保留原本「您確定將刪除此課程？」確認
- [ ] C. attendCount == 0、dcBindCount > 0：一次確認→先刪對應再刪課（不再跳第二次確認）
- [ ] D. dcBindCount < 0：提示並中止（不刪課）
- [ ] E. DeleteCourseDCBindByCourseID < 0：提示並中止（不刪課）

---

## Todo 4 — 更新文件：刪除課程功能調整0115.md
- [ ] 補上變更摘要（取消第二次確認、先刪對應再刪課）
- [ ] 記錄變更檔案與函式
- [ ] 填寫測試案例結果
