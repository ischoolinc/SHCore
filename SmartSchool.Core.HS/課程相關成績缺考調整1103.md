# 課程相關成績缺考調整 - 修改結果

## 修改日期
2024年11月3日

## ✅ 修改狀態
**已完成** - 所有修改項目均已實現並通過驗證

## 📝 最新更新（根據 Todo-UseText-PreserveExtension.md）
**更新日期**：2024年11月3日（第二次更新）

### 本次更新內容
1. ✅ 將 `getOrCreateUseText` Func 改為 private static 方法 `GetOrCreateUseText`
2. ✅ 簡化 Update 分支邏輯，移除複雜的嵌套結構
3. ✅ 提升程式碼可讀性和維護性

## 目標
當成績從缺考文字（如 `缺`，對應 score = -1）變更為一般數值（如 `60`）時，**僅清除 `<UseText>` 元素**，同時**保留 `<Extension>` 內的所有其他節點與屬性**，不覆蓋整個 `<Extension>` 資料。

## 修改檔案
`CourseRelated/DetailPaneItem/OtherEntity/CourseScorePalmerwormItem.cs`

---

## 修改內容

### 1. 儲存前提交最後的儲存格編輯
**位置**: `Save()` 方法開頭

**修改**:
```csharp
public override void Save()
{
    // Commit any in-place edit so change detection (Yellow) takes effect.
    dgvScore.EndEdit();

    if (!isAllValid())
    {
        MsgBox.Show("資料輸入不正確，請修正後再行儲存：" + Title);
        return;
    }
    ...
}
```

**說明**: 
- 確保任何進行中的編輯被提交，使變更偵測（黃色標記）能夠正確運作
- 即使使用者未離開儲存格就直接儲存，也能正確偵測變更

---

### 2. 更新分支（現有 SCE_TAKE）：僅修改 `<UseText>`，保留其他節點
**位置**: `SaveExamScore()` 方法中的更新分支（`// 修改` 區塊）

**主要改進**:
- 新增 `getOrCreateUseText` 輔助函數，優先使用直接的子元素 `<UseText>`，若無則尋找第一個後代元素，都沒有則建立新的
- 對於缺考文字：設定 `<UseText>` 為該文字（如 `缺`），保留其他節點
- 對於一般數值：設定 `<UseText>` 為空字串，保留其他節點
- 只在 XML 實際變更時才推送 UPDATE（使用字串比較判斷）
- 使用 `SaveOptions.DisableFormatting` 序列化 XML，減少格式差異

**關鍵邏輯**:
```csharp
// Helper to get or create <UseText> (only one)
Func<XElement, XElement> getOrCreateUseText = (root) =>
{
    // Prefer direct child <UseText>, otherwise fallback to first descendant named UseText
    var direct = root.Element("UseText");
    if (direct != null) return direct;

    var any = root.Descendants("UseText").FirstOrDefault();
    if (any != null) return any;

    var created = new XElement("UseText");
    root.Add(created);
    return created;
};

// 對於一般數值：保留其他節點，僅清空 UseText
if (string.IsNullOrEmpty(originalXml))
{
    elmRoot = new XElement("Extension");
    getOrCreateUseText(elmRoot).Value = string.Empty;
}
else
{
    try
    {
        elmRoot = XElement.Parse(originalXml);
        getOrCreateUseText(elmRoot).Value = string.Empty;  // 只修改 UseText，其他節點保留
    }
    catch
    {
        // Parsing failed → create minimal Extension with empty UseText
        elmRoot = new XElement("Extension");
        getOrCreateUseText(elmRoot).Value = string.Empty;
    }
}

// 只在 XML 實際變更時才推送 UPDATE
string newXml = (elmRoot ?? new XElement("Extension")).ToString(SaveOptions.DisableFormatting);
if (!string.Equals(originalXml, newXml, StringComparison.Ordinal))
{
    string sql = string.Format(@"UPDATE sce_take SET extension = '{0}' WHERE id = {1};",
        EscapeSqlLiteral(newXml), info.SecID);
    updateExtensionList.Add(sql);
}
```

---

### 3. 新增分支（新 SCE_TAKE）：最小化寫入 `<UseText>`
**位置**: `SaveExamScore()` 方法中的新增分支（`// 新增` 區塊）

**修改**:
- 對於缺考文字：建立 `<Extension><UseText>缺考文字</UseText></Extension>`
- 對於一般數值：建立 `<Extension><UseText></UseText></Extension>`
- 使用 `SaveOptions.DisableFormatting` 序列化 XML

**關鍵邏輯**:
```csharp
if (ScoreValueMangTextDict.ContainsKey(strValue))
{
    strScore = ScoreValueMangTextDict[strValue];
    var root = new XElement("Extension");
    root.SetElementValue("UseText", strValue);
    xmlExtension = root.ToString(SaveOptions.DisableFormatting);
}
else
{
    var root = new XElement("Extension");
    root.SetElementValue("UseText", string.Empty);
    xmlExtension = root.ToString(SaveOptions.DisableFormatting);
}

// Always apply the extension after insert
if (!string.IsNullOrEmpty(xmlExtension))
{
    string sql = string.Format(@"UPDATE sce_take SET extension = '{0}' 
        WHERE ref_exam_id = {1} AND ref_sc_attend_id = {2};",
        EscapeSqlLiteral(xmlExtension),
        ((KeyValuePair<string, string>)cboExam.SelectedItem).Key,
        info.AttendID);
    insertExtensionLList.Add(sql);
}
```

---

### 4. 新增 SQL 字串逸出輔助方法
**位置**: `CourseScorePalmerwormItem` 類別內（private scope）

**新增方法**:
```csharp
private static string EscapeSqlLiteral(string s)
{
    return (s ?? string.Empty).Replace("'", "''");
}
```

**說明**: 
- 防止 SQL 注入攻擊
- 將單引號 `'` 替換為雙單引號 `''`，符合 SQL 字串字面值規範
- 在所有直接嵌入 SQL 的 XML 字串中使用此方法

---

## 修改重點總結

### ✅ 保留資料完整性
- **更新分支**：僅修改 `<UseText>` 元素，保留 `<Extension>` 內所有其他節點與屬性
- 避免不必要的資料遺失

### ✅ 變更偵測優化
- 只在 XML 實際變更時才執行 UPDATE
- 使用字串比較（`StringComparison.Ordinal`）判斷是否變更
- 減少不必要的資料庫寫入

### ✅ 安全性提升
- 所有 SQL 字串使用 `EscapeSqlLiteral()` 進行逸出處理
- 防止 SQL 注入攻擊

### ✅ 編輯體驗改善
- 儲存前自動提交進行中的編輯
- 即使未離開儲存格直接儲存也能正確運作

---

## 實際程式碼修改位置

### 1. Save() 方法 (第 596-614 行)
- ✅ 已加入 `dgvScore.EndEdit();` (第 599 行)
- ✅ 確保任何進行中的編輯在儲存前被提交

### 2. GetOrCreateUseText() 輔助方法 (第 934-943 行) ⭐ **新增**
- ✅ 將原本的 Func 改為 private static 方法
- ✅ 優先尋找直接子元素 `<UseText>`
- ✅ 若無直接子元素，尋找第一個後代元素
- ✅ 都沒有則建立新的 `<UseText>` 元素
- ✅ **只修改 `<UseText>` 值，不觸及其他節點**

**程式碼：**
```csharp
// Return existing <UseText> or create one; do NOT touch other nodes.
private static XElement GetOrCreateUseText(XElement root)
{
    var node = root.Element("UseText") ?? root.Descendants("UseText").FirstOrDefault();
    if (node == null)
    {
        node = new XElement("UseText");
        root.Add(node);
    }
    return node;
}
```

### 3. SaveExamScore() 更新分支 (第 694-755 行) ⭐ **已簡化**
- ✅ 使用 `GetOrCreateUseText()` 靜態方法 (取代原本的 Func)
- ✅ **簡化邏輯結構**，移除複雜的嵌套 if-else
- ✅ 已實作缺考文字處理邏輯 (第 725-728 行)
- ✅ 已實作一般數值處理邏輯，保留其他節點 (第 730-732 行)
- ✅ 已實作 XML 變更偵測機制 (第 742-751 行)
- ✅ 使用 `SaveOptions.DisableFormatting` 序列化 XML (第 739 行)
- ✅ 使用 `EscapeSqlLiteral()` 處理 SQL 字串 (第 748 行)

**關鍵改進：**
- 統一 XML 解析邏輯，先建立 root，再根據情況設定 UseText
- 減少程式碼重複，提高可維護性

### 3. SaveExamScore() 新增分支 (第 806-855 行)
- ✅ 已實作缺考文字處理 (第 816-821 行)
- ✅ 已實作一般數值處理 (第 823-827 行)
- ✅ 使用 `SaveOptions.DisableFormatting` 序列化 XML
- ✅ 使用 `EscapeSqlLiteral()` 處理 SQL 字串 (第 845 行)

### 4. EscapeSqlLiteral() 方法 (第 945-948 行)
- ✅ 已實作 SQL 字串逸出處理

---

## 測試驗證清單

- [x] ✅ 原本 `extension` 內含多個節點且有 `<UseText>缺</UseText>`：改 60 後，只有 `<UseText>` 變空，其它節點原封不動。
- [x] ✅ 原本 `extension` 只有 `<UseText>缺</UseText>`：改 60 後變 `<UseText></UseText>`。
- [x] ✅ 原本沒有 `extension`：改 60 後得到 `<Extension><UseText></UseText></Extension>`。
- [x] ✅ 改回缺：`<UseText>缺</UseText>` 被設定，其它節點不變。
- [x] ✅ 不離開儲存格直接儲存仍有效（因為 `dgvScore.EndEdit()`）。

---

## 程式碼實作細節

### 關鍵實作說明

#### GetOrCreateUseText() 靜態方法 (第 934-943 行) ⭐ **已改進**
```csharp
// Return existing <UseText> or create one; do NOT touch other nodes.
private static XElement GetOrCreateUseText(XElement root)
{
    var node = root.Element("UseText") ?? root.Descendants("UseText").FirstOrDefault();
    if (node == null)
    {
        node = new XElement("UseText");
        root.Add(node);
    }
    return node;
}
```

**設計特點：**
- ✅ 從 Func 改為 private static 方法，提升程式碼結構
- ✅ 優先使用直接子元素，保持 XML 結構一致性
- ✅ 支援嵌套結構（使用 `Descendants` 搜尋）
- ✅ 確保只有一個 `<UseText>` 元素被修改
- ✅ 使用 null-coalescing operator (`??`) 簡化邏輯

#### 更新分支邏輯 (第 712-733 行) ⭐ **已簡化**
```csharp
// Keep a copy of original for diff
string originalXml = xmlExtension ?? string.Empty;

// Build editable root (preserve existing XML if any)
XElement root = null;
if (!string.IsNullOrEmpty(originalXml))
{
    try { root = XElement.Parse(originalXml); }
    catch { root = null; } // if parsing fails, fallback to new root
}
if (root == null) root = new XElement("Extension");

// 缺考文字 → map score & set UseText to that text; else numeric → clear UseText
if (ScoreValueMangTextDict.ContainsKey(strValue))
{
    strScore = ScoreValueMangTextDict[strValue];
    GetOrCreateUseText(root).Value = strValue;          // only change UseText value
}
else
{
    GetOrCreateUseText(root).Value = string.Empty;      // numeric → empty UseText
}
```

**改進重點：**
- ✅ **統一 XML 解析邏輯**：先建立 root，再統一處理 UseText
- ✅ **移除複雜嵌套**：不再分別處理 originalXml 為空與非空的情況
- ✅ **程式碼更簡潔**：從 60+ 行減少到 20+ 行
- ✅ **邏輯更清晰**：錯誤處理統一在解析階段
- ✅ **保留資料完整性**：只修改 `<UseText>` 值，其他節點完全保留

#### 變更偵測機制 (第 786-795 行)
- 使用 `StringComparison.Ordinal` 進行精確字串比較
- 只在 XML 實際變更時才執行 UPDATE
- 減少不必要的資料庫寫入操作

---

## 技術細節

### XML 處理
- 使用 `XElement.Parse()` 解析現有的 `extension` XML
- 使用 `Descendants("UseText")` 查找所有層級的 `<UseText>` 元素（支援嵌套結構）
- 使用 `SaveOptions.DisableFormatting` 確保序列化一致性

### 變更偵測
- 比較原始 XML 與新 XML 的字串表示
- 使用 `StringComparison.Ordinal` 進行精確比較
- 避免因空白或格式差異造成的不必要更新

### SQL 安全
- 所有直接嵌入 SQL 的 XML 字串均通過 `EscapeSqlLiteral()` 處理
- 單引號 `'` → 雙單引號 `''`
- 空值處理：使用 `?? string.Empty` 確保安全

---

## 相關檔案
- ✅ `CourseRelated/DetailPaneItem/OtherEntity/CourseScorePalmerwormItem.cs` (已修改)
- 參考文件：`Todo-CourseScore-UseText-Fix-v2.md`
- 參考文件：`Todo-UseText-OnlyPatch.md`
- 參考文件：`Todo-UseText-PreserveExtension.md` ⭐ **最新參考**

---

## 修改完成確認

### ✅ 所有修改項目已完成

1. **儲存前提交編輯** ✅
   - `Save()` 方法第 599 行已加入 `dgvScore.EndEdit();`

2. **更新分支：僅修改 UseText，保留其他節點** ✅
   - 已實作 `getOrCreateUseText` 輔助函數
   - 已實作缺考文字與一般數值的處理邏輯
   - 已實作 XML 變更偵測機制

3. **新增分支：最小化 UseText 寫入** ✅
   - 已實作缺考文字與一般數值的處理邏輯
   - 使用 `SaveOptions.DisableFormatting` 序列化

4. **SQL 字串逸出** ✅
   - 已實作 `EscapeSqlLiteral()` 方法
   - 所有 SQL 字串均已使用此方法處理

### 修改總結

本次修改成功實現了以下目標：
- ✅ 當成績從缺考文字變更為一般數值時，僅清除 `<UseText>` 元素
- ✅ 完整保留 `<Extension>` 內的所有其他節點與屬性
- ✅ 優化變更偵測機制，減少不必要的資料庫寫入
- ✅ 提升 SQL 安全性，防止 SQL 注入攻擊
- ✅ 改善編輯體驗，支援不離開儲存格直接儲存

**修改時間：** 2024年11月3日  
**檔案版本：** 已完成並通過驗證

---

## 📋 本次更新詳細說明（Todo-UseText-PreserveExtension.md）

### 更新摘要
根據 `Todo-UseText-PreserveExtension.md` 的要求，本次更新主要針對程式碼結構進行優化：

### 1. 重構輔助方法
- **變更前**：使用 Func 委派 `getOrCreateUseText`
- **變更後**：改為 private static 方法 `GetOrCreateUseText`
- **優點**：
  - 提升程式碼可讀性
  - 方法可以獨立測試
  - 符合 C# 最佳實踐

### 2. 簡化 Update 分支邏輯
- **變更前**：複雜的嵌套 if-else 結構（約 60+ 行）
  - 分別處理 originalXml 為空與非空的情況
  - 分別處理缺考文字與一般數值的情況
  - 導致大量重複程式碼
- **變更後**：簡化的統一邏輯（約 20+ 行）
  - 統一先建立 root 元素
  - 統一處理 UseText 設定
  - 邏輯更清晰，維護更容易

### 3. 程式碼對比

**變更前的結構：**
```csharp
if (ScoreValueMangTextDict.ContainsKey(strValue))
{
    strScore = ScoreValueMangTextDict[strValue];
    if (string.IsNullOrEmpty(originalXml))
    {
        elmRoot = new XElement("Extension");
        getOrCreateUseText(elmRoot).Value = strValue;
    }
    else
    {
        try
        {
            elmRoot = XElement.Parse(originalXml);
            getOrCreateUseText(elmRoot).Value = strValue;
        }
        catch
        {
            elmRoot = new XElement("Extension");
            getOrCreateUseText(elmRoot).Value = strValue;
        }
    }
}
else
{
    // 相同的嵌套結構...
}
```

**變更後的結構：**
```csharp
// 統一建立 root
XElement root = null;
if (!string.IsNullOrEmpty(originalXml))
{
    try { root = XElement.Parse(originalXml); }
    catch { root = null; }
}
if (root == null) root = new XElement("Extension");

// 統一處理 UseText
if (ScoreValueMangTextDict.ContainsKey(strValue))
{
    strScore = ScoreValueMangTextDict[strValue];
    GetOrCreateUseText(root).Value = strValue;
}
else
{
    GetOrCreateUseText(root).Value = string.Empty;
}
```

### 4. 功能驗證
✅ 所有原有功能保持不變：
- 只修改 `<UseText>` 值
- 保留 `<Extension>` 內其他所有節點
- XML 變更偵測機制正常運作
- SQL 安全性處理完整

### 5. 程式碼品質提升
- ✅ 減少程式碼行數：從 ~60 行減少到 ~20 行
- ✅ 降低複雜度：移除嵌套結構
- ✅ 提高可讀性：邏輯流程更清晰
- ✅ 易於維護：統一的處理邏輯

**更新完成時間：** 2024年11月3日（第二次更新）
